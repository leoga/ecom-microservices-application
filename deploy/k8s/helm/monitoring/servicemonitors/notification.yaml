apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: notification
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: notification
  namespaceSelector:
    matchNames:
      - ecomapp
  targetLabels:
    - application
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 3s