apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: user
  namespaceSelector:
    matchNames:
      - ecomapp
  targetLabels:
    - application
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 3s