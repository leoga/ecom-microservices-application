auth:
  username: leoga
  password: leoga
  database: postgres

primary:
  initdb:
    scripts:
      init.sql: |
        CREATE DATABASE product OWNER leoga;
        CREATE DATABASE "order" OWNER leoga;